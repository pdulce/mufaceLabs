server:
  port: 8086

spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/home/ubuntu/usr/develop/data/bd/h2/db;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: password
    #password: postgres
    #schema: classpath:schema.sql
    #url: jdbc:postgresql://localhost:5432/postgresdb
    #username: postgres

  h2:
    console:
      enabled: true



  kafka:
    consumer:
      bootstrap-servers: 127.0.0.1:29092
  redis:
    host: 127.0.0.1
    password: SUPER_SECRET_PASSWORD
    port: 6379
  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        format_sql: true