server:
  port: 8086

mail:
  password:
    encoded: ewzqlzdstujochya

spring:
  # command domain
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.137:3306/SAMPLECOMMANDDB
    username: test
    password: test

  # query domain
  redis:
    host: 192.168.1.137
    #password: samplepwd
    port: 6379

  # event domaindoc
  data:
    mongodb:
      host: 192.168.1.137
      port: 27017
      uri: mongodb://sampleuser:samplepwd@192.168.1.137:27017/queryStore
      authentication-database: admin  # Si estamos usando autenticación
    sql:
      init:
        mode: always
    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true
      properties:
        hibernate:
          format_sql: true

# Event bus configuration
  kafka:
    topic: topicCQRS
    consumer:
      bootstrap-servers: 192.168.1.137:29092
      group-id: cqrs
      auto-offset-reset: earliest
      properties:
        spring:
          json:
            trusted:
              packages: '*'
      producer:
        key-serializer: org.apache.kafka.common.serialization.StringSerializer
        value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

