server:
  port: 8086

axon:
  eventhandling:
    processors:
      kafkaProcessor:
        mode: tracking
        source: myKafkaEventProcessor
        initialSegmentCount: 1
        threadCount: 1

spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/home/ubuntu/usr/develop/data/bd/h2/db;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: password

  h2:
    console:
      enabled: true

  # MongoDB configuration
  spring:
    data:
      mongodb:
        uri: mongodb://192.168.1.128:27017/mi-event-db
        database: eventStore

  # Axon configuration
  axon:
    event-handling:
      processors:
        default:
          mode: tracking
          source: default

  data:
    mongodb:
      user: pedro
      password: passwd
      uri: mongodb://192.168.1.128:27017/db

  kafka:
    topic: myTopic
    consumer:
      bootstrap-servers: 192.168.1.128:9092
      group-id: my-group
      auto-offset-reset: earliest
      properties:
        spring:
          json:
            trusted:
              packages: '*'
      producer:
        key-serializer: org.apache.kafka.common.serialization.StringSerializer
        value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  #redis:
   # host: 127.0.0.1
   # password: SUPER_SECRET_PASSWORD
   # port: 6379

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql: true

mail:
  password:
    encoded: ewzqlzdstujochya